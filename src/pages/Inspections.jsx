import ResourcePage from '../components/ResourcePage'
import { inspectionConfig } from '../config/entityConfigs'

export default function Inspections() {
  return <ResourcePage config={inspectionConfig} />
}
